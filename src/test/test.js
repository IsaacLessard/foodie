var main = require('../app/main');
var assert = require('assert');

describe('phi', function () {
	it('should equate the correct correlation for a given table', function () {
		assert.equal(phi,([76,9,4,1]), 7)
	})
})
